server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway

  cloud:
    gateway:

      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "https://airbnb-1-bndh.onrender.com"
            allowedHeaders:
              - Authorization
              - Content-Type
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowCredentials: true

      routes:

        # AUTH → USER SERVICE (NO STRIP)
        - id: user-service
          uri: ${USER_SERVICE_URL}
          predicates:
            - Path=/auth/**
          filters:
            - RemoveRequestHeader=Cookie

        # LISTING → LISTING SERVICE (NO STRIP)
        - id: listing-service
          uri: ${LISTING_SERVICE_URL}
          predicates:
            - Path=/listing/**
          filters:
            - RemoveRequestHeader=Cookie

jwt:
  secret: ${JWT_SECRET}
